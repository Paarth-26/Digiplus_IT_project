fastapi==0.141.1
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
python-dotenv==1.2.2
groq==1.6.0

# Seed script only — the fallback path in seed_kb.py works without it.
datasets==5.0.1

# The frontend is a separate React app; see frontend/README.md. It talks to this
# backend over HTTP from the browser, so it adds nothing to this file.
